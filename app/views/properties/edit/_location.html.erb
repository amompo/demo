<%= form_for property do |f| %>
  <%= render partial: 'properties/edit/location/top',    
             locals: {mode: mode, property: property, form: f }
  %>

  <%= render partial: 'properties/edit/location/bottom', 
             locals: {mode: mode, property: property, form: f }
  %>
<% end %>