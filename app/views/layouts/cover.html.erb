<!DOCTYPE html>
<html lang="en">
  <head>
    <%= render partial: 'layouts/application/head' %>
  </head>
  <body>
    <%= render partial: 'layouts/cover/header' %>

    <%= yield %>

    <%= render 'layouts/bars/footer' %>

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
